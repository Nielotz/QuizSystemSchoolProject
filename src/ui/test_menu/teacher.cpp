#include "../../headers/ui/test_menu/teacher.h"

void ui::test_menu::Teacher::show(string test_name, string username, size_t amount_of_questions, unordered_map<string, int> users_points,
                                  string selected_answer)
{
    // TODO: Show teacher test menu.
}
