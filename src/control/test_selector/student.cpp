#include "../../headers/control/test_selector/student.h"

namespace Control
{
    namespace TestSelector
    {
        string Student::control_test_selection(vector<string> tests_data)
        {
            string s;
            return s;
        }
    }
}